# service-billing

Terraform for the billing service infrastructure. Handles invoicing and
payment records. Consumes shared modules from infra-modules.

NOTE: pinned to vpc-base v1.0.0 - has not yet picked up the v2.0.0 flow logs
upgrade that webshop and analytics are already on.
